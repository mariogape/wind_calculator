# Core scientific + plotting
numpy>=1.22
matplotlib>=3.6
netCDF4>=1.6

# Widgets
ipywidgets>=8

# Raster + vector IO
# Rasterio 1.5+ publishes wheels for Python 3.12+.
# Keep 1.3.x for older interpreters used by the original notebooks.
rasterio>=1.3,<1.4; python_version < "3.12"
rasterio>=1.5; python_version >= "3.12"
pyogrio>=0.11
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
requests>=2.31
cdsapi>=0.7

# Optional but recommended: folder picker widget
ipyfilechooser>=0.6

# LiDAR IO
laspy[lazrs]>=2.5

# Animation
imageio>=2.28
imageio-ffmpeg>=0.4.9
